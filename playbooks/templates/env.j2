# API
NODE_ENV={{ node_env }}
PORT={{ api_port }}
DBINIFED={{ db_url }}
SECRET_KEY={{ secret_key }}
LIMIT_BODY={{ limit_body }}
TZ={{ tz }}
CODE={{ code }}

sar_base_dir: {{SAR_BASE_DIR}}
image_name: {{IMAGE_NAME}}
image_tag: {{IMAGE_TAG}}